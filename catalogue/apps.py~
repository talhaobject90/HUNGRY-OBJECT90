from django.apps import AppConfig

class CatalogueConfig(AppConfig):
    name = 'catalogue'
    label = 'my.catalogue'  # <-- this is the important line - change it to anything other than the default ('foo' in this case)
